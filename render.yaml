services:
  - type: web
    name: client-status-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TOKEN
        sync: false
      - key: CLIENT_TTL_MS
        value: 120000
